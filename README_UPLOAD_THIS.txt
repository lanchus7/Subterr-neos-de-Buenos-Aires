IMPORTANT:
Upload these files DIRECTLY to the ROOT of the existing GitHub repository:
- index.html
- styles.css
- carousel.js
- header.webp
- mid.webp
- lower.webp

Do not upload this folder as a folder.
GitHub should show these six filenames at the same level as your assets folder.

This patch intentionally uses the SAME filenames currently served by your live site,
so they replace the black-background files instead of adding differently named copies.
