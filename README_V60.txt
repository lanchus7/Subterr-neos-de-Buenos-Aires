V60 — Desplegables mobile corregidos

Base: V59.

Se rehizo únicamente el layout móvil de las estaciones:
- combinaciones = columna izquierda
- nodos = columna central
- nombres = columna derecha
- la línea vertical usa el mismo eje exacto que los nodos

Esto elimina los offsets negativos que Safari estaba interpretando mal
y evita que los nombres, circulitos y bochitas se corran entre sí.

Desktop no cambia.
